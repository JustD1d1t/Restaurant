import classes from "./Banner.module.scss";
const Banner = () => {
  return (
    <div className={`${classes.banner}`}>
      <h1 className="h1">"Loggia - we love food"</h1>
    </div>
  );
};

export default Banner;
